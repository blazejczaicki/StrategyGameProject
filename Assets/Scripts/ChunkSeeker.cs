using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class ChunkSeeker
{
    //public static Chunk CheckOnWhichChunkYouStayed(Vector2 position)
    //{
    //    Vector2 newCurrentChunkPos = new Vector2();
    //    newCurrentChunkPos.x = Mathf.Round(position.x / Chunk.chunkSize) * Chunk.chunkSize;
    //    newCurrentChunkPos.y = Mathf.Round(position.y / Chunk.chunkSize) * Chunk.chunkSize;
    //    return GameManager.instance.getChunkTransform(newCurrentChunkPos).GetComponent<Chunk>();
    //}
}
